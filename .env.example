# Variáveis de ambiente para deploy

# Backend
DATABASE_URL=postgresql://usuario:senha@host:porta/database
JWT_SECRET=sua-chave-secreta-jwt-aqui
NODE_ENV=production

# Frontend (deixar vazio para mesma origem)
VITE_API_URL=
